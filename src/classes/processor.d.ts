import Application from './application'

/**
 * 以应用程序为中心的处理器类
 */
class Processor {
    /**
     * 
     * @param app 应用程序实例
     */
    constructor(public app: Application)
}

export = Processor